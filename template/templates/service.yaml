apiVersion: v1
kind: Service
metadata:
  name: template-service
  namespace: {{ .Values.namespace }}
spec:
  type: LoadBalancer
  selector:
    app: template
  ports:
    - protocol: TCP
      name: service-port
      port: 8080
      targetPort: 8080
